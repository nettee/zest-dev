[project]
name = "zest-dev-e2e"
version = "0.1.0"
description = "Python E2E tests for the zest-dev CLI"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.0.0",
  "PyYAML>=6.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
